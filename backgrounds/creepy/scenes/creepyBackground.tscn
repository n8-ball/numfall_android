[gd_scene load_steps=8 format=2]

[ext_resource path="res://backgrounds/creepy/sprites/creepyGrid.png" type="Texture" id=1]
[ext_resource path="res://backgrounds/creepy/sprites/creepyForestInner.png" type="Texture" id=2]
[ext_resource path="res://backgrounds/default/scripts/defaultBackground.gd" type="Script" id=3]
[ext_resource path="res://backgrounds/creepy/sprites/creepyForestBorder.png" type="Texture" id=4]
[ext_resource path="res://backgrounds/creepy/scripts/grain.gd" type="Script" id=5]

[sub_resource type="Shader" id=1]
code = "shader_type canvas_item;

uniform float seed = 2.0;
const float strength = 0.4;

float rand(vec2 uv) {
	uv = mod(uv, 10000.0);
    return fract(sin(dot(uv * seed, vec2(12.9898,78.233))) * 43758.5453);
}

void fragment() {
	vec2 coord = UV;
	
	float value = rand(coord);
	value = (value - 0.5) * strength;
	
	COLOR = vec4(texture(TEXTURE, UV) + vec4(value, value, value, 0.0));
}"

[sub_resource type="ShaderMaterial" id=2]
shader = SubResource( 1 )
shader_param/seed = 1.0

[node name="background" type="Node2D"]
pause_mode = 2
z_index = -3
z_as_relative = false
script = ExtResource( 3 )

[node name="forest" type="Sprite" parent="."]
material = SubResource( 2 )
texture = ExtResource( 2 )
centered = false
script = ExtResource( 5 )

[node name="border" type="Sprite" parent="."]
texture = ExtResource( 4 )
centered = false

[node name="grid" type="Sprite" parent="."]
texture = ExtResource( 1 )
centered = false
