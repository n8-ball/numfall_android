[gd_scene load_steps=14 format=2]

[ext_resource path="res://menu/sprites/yesButtonPressed.png" type="Texture" id=1]
[ext_resource path="res://menu/sprites/noButtonPressed.png" type="Texture" id=2]
[ext_resource path="res://menu/sprites/noButtonNormal.png" type="Texture" id=3]
[ext_resource path="res://menu/sprites/yesButtonNormal.png" type="Texture" id=4]
[ext_resource path="res://boosts/sprites/continueBoostMenu.png" type="Texture" id=5]
[ext_resource path="res://debug/sounds/deny.wav" type="AudioStream" id=6]
[ext_resource path="res://debug/sounds/confirm.wav" type="AudioStream" id=8]

[sub_resource type="GDScript" id=5]
script/source = "extends Node2D

onready var animator : AnimationPlayer = $\"animator\"
onready var board : Node2D = $\"..\"

var is_open = false

func open():
	animator.play(\"open\")
	is_open = true

func close():
	animator.play_backwards(\"open\")
	is_open = false

func is_open():
	return is_open

func yes():
	if !animator.is_playing():
		continueBoost()
		board.continueBoosts -= 1
		resumeGame()
		close()

func no():
	if !animator.is_playing():
		close()
		board.menu.endGameMenu()

func continueBoost():
	var deleteCount = 0
	var pieceMinimum = board.spawner.floorNum
	board.boostActive = true
	while deleteCount < 15:
		for y in range(board.brdHt - 1, -1, -1):
			for x in range(board.brdWd):
				var curPiece = board.board[y][x]
				if is_instance_valid(curPiece) and curPiece.value == pieceMinimum\\
				&& deleteCount < 15:
					deleteCount += 1
					deletePiece(Vector2(x, y))
		pieceMinimum += 1

func deletePiece(vec):
	var curPiece = board.board[vec.y][vec.x]
	if is_instance_valid(curPiece):
		curPiece.setState(curPiece.DELETE_STATE)

func resumeGame():
	board.setGameOver(false)
	board.saveReady = true
	board.saveLoad.saveGame()
	board.boostActive = false
"

[sub_resource type="Animation" id=1]
resource_name = "open"
length = 0.2
step = 0.05
tracks/0/type = "value"
tracks/0/path = NodePath(".:scale")
tracks/0/interp = 1
tracks/0/loop_wrap = true
tracks/0/imported = false
tracks/0/enabled = true
tracks/0/keys = {
"times": PoolRealArray( 0, 0.05, 0.1, 0.15, 0.2 ),
"transitions": PoolRealArray( 1, 1, 1, 1, 1 ),
"update": 0,
"values": [ Vector2( 0, 0 ), Vector2( 0.2, 0.2 ), Vector2( 0.7, 0.7 ), Vector2( 1.2, 1.2 ), Vector2( 1, 1 ) ]
}

[sub_resource type="GDScript" id=3]
script/source = "extends TextureButton

onready var parent : Node2D = $\"../..\"

func _pressed():
	if is_instance_valid(parent):
		parent.no()
		find_node(\"noSound\").play()
"

[sub_resource type="GDScript" id=2]
script/source = "extends TextureButton

onready var parent : Node2D = $\"../..\"
onready var animator : AnimationPlayer = $\"animator\"

var appStore

func _ready():
	if Engine.has_singleton(\"InAppStore\"):
		appStore = Engine.get_singleton(\"InAppStore\")

func _pressed():
	if is_instance_valid(parent) and parent.board.continueBoosts > 0:
		parent.yes()
		find_node(\"yesSound\").play()
	elif is_instance_valid(parent):	
		if is_instance_valid(appStore):
			var result = appStore.purchase( { \"product_id\" : \"continue.boost\" } )
			if result == OK:
				print(\"ContinueBoost: OK\")
				animator.play(\"busy\") # show the \"waiting for response\" animation
			else:
				print(\"ContinueBoost: \" + str(result))
				animator.play(\"error\")
		
		else:
			print(\"InAppStore Singleton does not exist\")
"

[sub_resource type="Animation" id=6]
resource_name = "busy"

[sub_resource type="Animation" id=7]
resource_name = "error"

[node name="continueBoostMenu" type="Node2D"]
rotation = -0.00254742
scale = Vector2( 1e-05, 1e-05 )
z_index = 7
script = SubResource( 5 )

[node name="animator" type="AnimationPlayer" parent="."]
anims/open = SubResource( 1 )

[node name="continueBoostPopup" type="Sprite" parent="."]
texture = ExtResource( 5 )

[node name="noButton" type="TextureButton" parent="continueBoostPopup"]
margin_left = 25.0
margin_top = 150.0
margin_right = 441.0
margin_bottom = 351.0
texture_normal = ExtResource( 3 )
texture_pressed = ExtResource( 2 )
script = SubResource( 3 )
__meta__ = {
"_edit_use_anchors_": false
}

[node name="noSound" type="AudioStreamPlayer" parent="continueBoostPopup/noButton"]
stream = ExtResource( 6 )

[node name="yesButton" type="TextureButton" parent="continueBoostPopup"]
margin_left = -440.0
margin_top = 150.0
margin_right = -23.9999
margin_bottom = 351.0
texture_normal = ExtResource( 4 )
texture_pressed = ExtResource( 1 )
script = SubResource( 2 )
__meta__ = {
"_edit_use_anchors_": false
}

[node name="yesSound" type="AudioStreamPlayer" parent="continueBoostPopup/yesButton"]
stream = ExtResource( 8 )

[node name="animator" type="AnimationPlayer" parent="continueBoostPopup/yesButton"]
anims/busy = SubResource( 6 )
anims/error = SubResource( 7 )
